version: 2.1
jobs:
  test:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run:
          command: mvn test
      - store_test_results:
          path: target/surefire-reports
workflows:
  build-and-test:
    jobs:
      - test-java